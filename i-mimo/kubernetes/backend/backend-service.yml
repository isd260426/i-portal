apiVersion: v1
kind: Service
metadata:
  name: i-mimo-backend-service
spec:
  type: NodePort
  selector:
    app: i-mimo-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30200 # NodePort mapping for external UI requests
